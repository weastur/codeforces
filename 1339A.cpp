#include <iostream>

using namespace std;

long long t, n, ans;

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);
	cin >> t;
	while (t--)
	{
		cin >> n;
		cout << n << endl;
	}
	return 0;
}
